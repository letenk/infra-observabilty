GRAFANA_BIND_IP=        # IP address to bind Grafana port (e.g. private/VPN IP). Never use 0.0.0.0
GRAFANA_PORT=3000       # Port to expose Grafana on the host (default: 3000)
GRAFANA_ADMIN_PASSWORD= # Strong password for Grafana admin user
